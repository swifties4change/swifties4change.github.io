{#
/**
 * @file
 * Default theme implementation for a field.
 *
 * We don't need to show this field, this is a workaround to hide this field
 * since we don't have the possibility to do it through admin.
 *
 * @ingroup themeable
 */
#}
